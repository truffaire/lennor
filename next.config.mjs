/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static marketing assets live in /public and are referenced by absolute
  // paths (e.g. /assets/img/...). No custom config needed beyond defaults.
  reactStrictMode: true,
};

export default nextConfig;
